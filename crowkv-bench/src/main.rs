//! `crowkv-bench` — `CrowKV` benchmark and load test tool.
//!
//! Runs performance workloads against a `crowkv-server` instance or cluster.
//! Real content lands in P4 (G4 milestone). See `doc/plan/plan-rpc.md` and
//! `doc/test/test-design.md` §14.1 (crowbench architecture).

fn main() {
    eprintln!("crowkv-bench: not yet implemented (lands in P4)");
    std::process::exit(1);
}
