//! `CrowKV` gRPC services and client library.
//!
//! Skeleton module. Real content lands in P4 per `doc/plan-rpc.md`:
//! `PeerService`, `VoteService`, `SnapshotService`, plus the client library
//! that owns topology cache, retry, and `NotLeaderHint` handling.
